[project]
name = "ai-ecommerce-app"
version = "0.1.0"
description = "AI-powered e-commerce DDD monolith"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "psycopg2-binary",
  "python-jose[cryptography]",
  "passlib[bcrypt]",
  "pydantic",
  "pgvector",
]
